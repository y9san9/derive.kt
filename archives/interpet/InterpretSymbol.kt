package interpret

/**
 * Either function or type (make interpretsymbol to be interpretsymbol.function
 * in the backend interface and use interpretsymbol.type for parameters and
 * everything else).
 *
 * For function allow it to be member function, simple function,
 * extension function. Maybe static dispatch / dynamic dispatch with receivers.
 */
public interface InterpretSymbol {
}
