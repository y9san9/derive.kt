# Interpret

Library to create custom interpreters over existing Kotlin functions.

Initial version targets derivatives calculation, later it might also support types introspection (what is already possible with kotlinx.serialization).

Then it will be possible to send Kotlin functions over the Internet, execute them in a distributed manner and do a bunch of other cool stuff!
